{
  "name": "vale-desktop-electron",
  "version": "0.2.0",
  "description": "Vale Desktop — Electron shell over the local vale-agent service (SecureCRT-style multi-session workspace: terminal / SSH / serial / browser) with AI control. Stage-m: TS main process, agent lifecycle in Rust.",
  "main": "src/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && electron .",
    "dist": "npm run build && electron-builder",
    "test": "node --test"
  },
  "license": "Apache-2.0",
  "devDependencies": {
    "electron": "^33.4.11",
    "electron-builder": "^25.0.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  },
  "allowScripts": {
    "electron@33.4.11": true
  }
}
