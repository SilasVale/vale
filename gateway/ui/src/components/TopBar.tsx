export default function TopBar() {
  return (
    <header className="topbar">
      <h1 className="topbar-title">Vale Gate</h1>
      <span className="topbar-status">Connected</span>
    </header>
  );
}
